# Independent QA audit — 2026-09-24 (evening, IST)

Auditor: Bodhi (parent agent). Scope: the all-India coordinator's in-flight
holdings. Read-only pass; no coordinator files were modified.

## What was checked

| Check | Result |
|---|---|
| Inventory schema (`data/inventory/inventory.jsonl`, 381 rows) | All 10 required columns present on every row (`state, district, ac_number, ac_name, part_number, roll_type, file_url, format, scriptable, last_updated`). No missing columns. |
| Fetched-status honesty | 132 `Y`, 177 fetch-failed http_000 (site unreachable), 17 http_406 (WAF), 35 excluded (separate agent), 11 not attempted, 9 not scriptable. Failures are logged with reasons — nothing is presented as fetched that was not. |
| Plaintext EPICs in parsed rows (`data/parsed/**/*.jsonl`) | **0 hits** across 64,343 parsed rows. Regex `[A-Z]{3}\d{7}` against all string fields except `epic_masked`. |
| `epic_masked` field format | 64,343 rows checked; every non-null value contains `*`. Zero unmasked-shaped values. |
| PDF validity (sample) | 6/6 sampled fetched PDFs begin with `%PDF` magic bytes. |
| URL fabrication spot-check | Fetched-`Y` URLs match observed official CEO patterns (e.g. `ceoarunachal.nic.in/archiverolls/EROLL2006/...`). No guessed URLs detected in sample. |

## Known in-flight gaps (not defects — parsing is mid-run)

- Arunachal Pradesh: 36 PDFs on disk, 1 PDF parsed so far (363 rows). Parsing continues.
- Jharkhand: 63,209 rows parsed, 69 rejects (legend/footer lines, correctly excluded).
- Delhi 2002 rows carry `epic_masked: null` — source PDFs appear not to carry EPICs in the parsed text; nulls render as "NOT AVAILABLE IN SOURCE DATA", which is honest. Flag: confirm at shard time that this is source-absence, not a parser miss.

## Field-contract fix (sent to coordinator)

`/vote-check` and `lib/shards.ts` expect `inv-rolls` shard rows as
`voter_name, relative_name, source_url, vintage_label, booth_no, source_label, vintage_class, record_type`.
The parser emits `elector_name, relation_name, source_file_url, roll_vintage, part_number, roll_type`.
Coordinator was instructed to map fields at shard-build time; otherwise the
vote-check page will not activate when `inv-rolls` lands.

## Verdict

Holdings are honest and privacy-safe so far. No fabrication, no unmasked
EPICs, failures logged rather than hidden. Re-audit after the unified index
lands and before production deploy.

## Wave-2 close-out decisions (2026-09-24, ~19:30 IST)

- **A0160008_1.pdf (Meghalaya): ACCEPT THE DROP, no refetch.** It is a
  truncated corrupt copy (missing trailer/xref, 0 extractable words) of a URL
  already covered by the good wave-2 fetch `A0160008.pdf` (3.57MB, valid).
  Refetching would only re-download the same bytes under a different name —
  no new data is at stake. The inventory row for the corrupt copy keeps its
  `fetched=Y (corrupt, superseded by A0160008.pdf)` note.
- **Meghalaya 12 PDFs:** embedded-OCR text layer is garbled (words out of
  logical row order, y-positions corrupted); three reconstruction strategies
  all produced cross-row pollution. Tesseract re-OCR attempted — not
  installable in this sandbox (no package). Options remaining: (a) OCR on a
  machine with Tesseract, (b) refetch from an Indian connection in case the
  source files differ, (c) leave unparsed with the logged reason. Currently
  (c) — honestly logged, not silently dropped.
- **jsDelivr cache lag:** wave-2 shards pushed to GitHub main (commit
  60aa69f8) but jsDelivr served the stale manifest until a manual purge.
  Lesson: after every shard push, purge
  `https://purge.jsdelivr.net/gh/williamtheconqueror48-art/sir-watch@main/data/shards/manifest.json`
  and re-verify the new dataset keys before claiming "live".
